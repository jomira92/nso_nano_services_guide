This is a generated Python package, made by:

  ncs-make-package --nano-service-skeleton python-and-template \
                   --component-class main.Main NTP

It contains a dummy YANG model which implements a minimal Service
that doesn't really do anything useful. They are there just to get
you going.
